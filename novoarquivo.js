var cadastros, index;

function soltar() {
    this.modal();
    this.cacelarAbrir();
    this.fecharSalvar();
    this.adcionar();
};

//let bancoDados = JSON.parse(localStorage.getItem('bancoDados') || '[]')
  
 
 // localStorage.setItem('bancoDados', JSON.stringify(bancoDados))
 // bancoDados = JSON.parse(localStorage.getItem('bancoDados'))
  


function inicia(nome,idade,sexo,opcoes) {
  
 localStorage.setItem('bancoDados', JSON.stringify(bancoDados))
  
}

function save(nome,idade,sexo,opcoes){
   // localStorage.setItem('bancoDados', JSON.stringify(bancoDados))
//}
//window.addEventListener('load',e => {
   // editarCadastros();
//})

for(item of bancoDados){

const pos=bancoDados.indexOf(item);

}
}

